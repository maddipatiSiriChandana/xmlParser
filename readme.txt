//To build the jar go to build.gradle location and  run the below command
gradle build


//To run the jar file run  the below command
java -jar C:/xmlParser/build/libs/XMLParser.jar

//To run gradle for Junit test cases go to build.gradle location  run the below command

gradle test
